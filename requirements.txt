# ============================================================
# requirements.txt — Bridge Bidding RL (ruben728/brl fork)
# ============================================================
# IMPORTANT: Do NOT simply run `pip install -r requirements.txt`
# JAX must be installed separately first with CUDA support.
# Follow the installation instructions in README.md exactly.
# ============================================================

# Core ML frameworks (install JAX separately — see README)
dm-haiku==0.0.16
optax==0.2.4
distrax==0.1.5
chex==0.1.88

# Bridge environment
pgx==2.4.0

# OpenSpiel (for supervised learning data pipeline)
open-spiel==1.4

# Config and validation
omegaconf==2.3.0
pydantic==2.5.3

# Numerical
numpy==1.26.3

# Logging
wandb==0.16.1

# Utilities
absl-py==2.0.0
dm-tree==0.1.8
toolz==0.12.0
